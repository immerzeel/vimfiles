Vim Setup
=========
Setup for my Vim.
I use Cygwin's version of vim, for the convenience of the unix tools I use
often.

WINDOWS
-------
### INSTALL
`ln -s ~/vimfiles > ~\.vim`
`ln -s ~/vimfiles/.vimrc > ~\.vimrc`
