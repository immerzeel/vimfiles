My Vimfiles settings and bundles.


WINDOWS
    INSTALL
        echo source ~/vimfiles/_vimrc > %HOME%\_vimrc

    UPDATE
        cd <home_dir>\vimfiles
        git submodule update --init

    (Remember to replace the '\\' with '/' in .gitmodules)
