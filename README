My Vimfiles settings and bundles.


WINDOWS
    INSTALL
        cd <home_dir>
        mklink /H vimfiles\vimrc _vimrc

    UPDATE
        cd <home_dir>\vimfiles\bundles
        git submodule init
        git submodule update
